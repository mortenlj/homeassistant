version: '2'

services:
  home-assistant:
    image: mortenlj/home-assistant-rpi:latest
    networks:
      - web
    labels:
      - "traefik.backend=home-assistant"
      - "traefik.frontend.rule=Host:home-assistant"

networks:
  web:
    external:
      name: traefik_webgateway
